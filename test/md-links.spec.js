


/*
jest.mock('../data.js', () => {
  path: jest.fn();
})

describe('convertRelative', () => {
  it('Deberia llamar a la path.Absolute', () => {
    expect(convertRelative().toHaveBeenCalled(path.isAbsolute))
  })
})

describe('mdLinks', () => {

  it('Deberia devolver una promesa', () => {
    expect(mdLinks()).toBe(Promise);
  });

  it('Debe rechazar la promesa cuando el path no existe', () => {
    return mdLinks('READMES.md').catch((error) => {
      expect(error).toBe('La ruta no existe');
    })
  })

});

*/
